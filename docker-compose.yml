version: '2'
services:
    nginx:
        build:
            dockerfile: ./docker/nginx.dockerfile
            context: .
        container_name: nginx
        ports:
            - "80:80"
    node:
        build:
            dockerfile: ./docker/node-api.dockerfile
            context: .
        container_name: node
        ports:
            - "8080:3000"
          